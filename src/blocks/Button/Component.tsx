import type { ButtonBlock as ButtonBlockProps } from 'src/payload-types'

type Props = {
  className?: string
} & ButtonBlockProps

export const ButtonBlock = ({ className }: Props) => {
  return <div />
}
